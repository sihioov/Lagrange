global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: api-server
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["api-server:8080"]
        labels:
          service: api-server
