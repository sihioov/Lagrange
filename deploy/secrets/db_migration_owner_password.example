# db_migration_owner_password — replace this placeholder with a unique,
# generated password for the migration_owner role. Keep it separate from
# postgres_password (the cluster administrator credential). Never commit the real file.
REPLACE_WITH_A_RANDOM_MIGRATION_OWNER_PASSWORD
