# cursor_secret — replace this placeholder with exactly 32 random bytes,
# encoded as one line (base64 or hex); this is the API minimum and fixed width.
# Never commit the real file.
REPLACE_WITH_EXACTLY_32_RANDOM_BYTES_BASE64_OR_HEX
