# Real secret files are never committed. Only placeholders below are kept.
*
!.gitignore
!README.md
!*.example
!provision-runtime-secrets.sh
!runtime-static-check.sh
!tls/
!tls/**/*.example
# Native-Linux service-specific copies are generated locally and never tracked.
runtime/
