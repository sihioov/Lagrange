[package]
name = "lagrange-portfolio-model"
description = "Lagrange Station shared sizing, costs, and replayable ledger invariants."
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]