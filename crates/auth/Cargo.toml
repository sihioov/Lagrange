[package]
name = "auth"
description = "Lagrange Station authentication and authorization (Auth0 OIDC, sessions, RBAC)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]